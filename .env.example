VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=

# Server-only Netlify Function configuration. Never prefix this with VITE_ or expose it to the browser.
SUPABASE_SERVICE_ROLE_KEY=
# Optional comma-separated browser origins allowed to call the account deletion function.
ALLOWED_ORIGINS=